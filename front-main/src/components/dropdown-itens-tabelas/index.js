import React from 'react';
import {
    Badge,
    Button,
    ButtonGroup,
    Card,
    Col,
    Dropdown,
    Row,
} from 'react-bootstrap';
import { CircularProgressbar } from 'react-circular-progressbar';
import CsLineIcons from '../../cs-line-icons/CsLineIcons';
import {
    coreMarkersTextNumber,
    Money,
    statusEsteiraProposta,
    tipoIconRegra,
} from '../../utils';

const DropdownItensTabelas = () => {
    return <div />;
};

export default DropdownItensTabelas;
